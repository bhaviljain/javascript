const arr =[1,2,3,4]
console.log(arr.slice(2,3)); //starting index - last index
console.log(arr); //slice does no changes the original array

const arr2 =[1,2,3,4]
console.log(arr.splice(1,3)); //deletes the 1,2,3
console.log(arr);//splice does  changes the original array


console.log(typeof 'typeof');